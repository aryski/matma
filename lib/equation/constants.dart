import 'package:flutter/material.dart';

const double boardRadius = textHgt / 20;
const double numberRadius = textHgt / 20;
const Offset numberRatio = Offset(textHgt * 3 / 4, textHgt);
const Offset signRatio = Offset(textHgt / 2, textHgt);
const double textHgt = 112.0;
const double fontSize = 90.0;
const double fontSizeSmall = 45.0;
const int smallDelayInMillis = 30;
const String addMsg = "+1";
const String subMsg = "−1";
