import 'package:flutter/material.dart';

const double boardRadius = numberHeightRatio / 20;
const double numberRadius = numberHeightRatio / 20;
const Offset numberRatio = Offset(numberHeightRatio * 3 / 4, numberHeightRatio);
const Offset signRatio = Offset(numberHeightRatio / 2, numberHeightRatio);
const double numberHeightRatio = 112.0;
const int smallDelayInMillis = 30;
